# Local/runtime outputs — do not publish machine snapshots.
runtime/*.txt
runtime/*.json
runtime/*.log

# Local planning/context artifacts — keep them on the operator machine only.
CONTEXT.md
.omx/

# Python/editor noise
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
.DS_Store
*.egg-info/
.venv/
